namespace StyloMail.Desktop;

/// <summary>
/// The process entry point.
/// </summary>
/// <remarks>
/// A placeholder until the Avalonia shell lands: something has to be here for
/// the project to build as the executable it will become, and inventing a
/// different shape now only to replace it later would be work whose only
/// product is a diff.
/// </remarks>
internal static class Program
{
    [STAThread]
    public static int Main(string[] args) => 0;
}
