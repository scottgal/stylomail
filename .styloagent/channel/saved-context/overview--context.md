# overview- resume doc, moved

**Read `overview-context.md` in this directory.** That is the single-dash path the cockpit names when
it tells `overview-` to checkpoint, and the one a compaction reloads.

This double-dash file was the only one that existed for most of the session, so it accumulated the
live content while the reload was looking for the other name. Its content has been moved rather than
duplicated: two resume docs that both claim to be current is how the stale one gets read.

Nothing else lives here.
