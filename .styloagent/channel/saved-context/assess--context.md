# `assess-` — checkpoint (moved)

**This file is a pointer. The authoritative checkpoint is [`assess-context.md`](./assess-context.md).**

It was moved on 2026-09-22 at the operator's direction. The previous contents are not preserved here
because they would immediately go stale — and a stale second copy is the failure this project spent the
day removing: two files that are supposed to agree, where the divergence is invisible from either one.

If anything still reads this path expecting the full checkpoint, point it at `assess-context.md` rather
than restoring a duplicate.
