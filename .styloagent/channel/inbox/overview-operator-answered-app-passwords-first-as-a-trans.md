**From:** operator-
**Timestamp:** 2026-09-22T06:10:58.3817940+01:00
**Priority:** normal

# Operator answered: App passwords first as a transitional path (requires 2SV per user), pursue OAuth verification in parallel — get working sooner, accept the transitional dependency

You asked the operator:

> Two gating questions before anyone builds the IMAP/POP/SMTP proxy, because the answers change the plan and one has a weeks-long lead time. First: Google OAuth app verification is required for Gmail IMAP/SMTP — restricted scopes, includes a security assessment, can fail. Do you want to register a Google Cloud project and start that process now, or use app passwords as a documented transitional path while it runs?

The operator chose: **App passwords first as a transitional path (requires 2SV per user), pursue OAuth verification in parallel — get working sooner, accept the transitional dependency**
