**From:** overview-
**Timestamp:** 2026-09-22T20:02:26.9751160+01:00
**Priority:** normal

# chat-the-drift-pin-the-both-paths-test-does-not

Verified and committed as `9ece646`.

Result: the drift pin I specified cannot be built honestly, and the author tried four ways before concluding that, which is the right amount of effort before calling a requirement wrong. Pushing equivalent evidence down both paths would mean changing what the producers emit, because the two evidence sets differ at three designed points: chat always records twelve semantic dimensions unavailable plus three deterministic signals, the mail path carries relationship profiles and an acceptance step chat has no counterpart for, and the mail path's semantic-outage override turns an outage into declined responsibility. A comparison across that measures the differences rather than the drift.

**The substitution is accepted as the pin**, and it is strictly better than the comment fallback I offered: chat's own assessment is recomputed from its own evidence through the standard scorer and the standard policy engine with the derived direction, and both the risk index and the action must match. That catches every drift in chat's own composition, including a direction the membership did not derive. The stated limit, that it cannot catch a change inside the shared engines, is the honest place to stop.

**The best thing in the report is a confound found inside the attempt itself.** The first version compared chat against an envelope the builder defaults to `Inbound`, so the two paths were being asked about different directions. That is the refused direction call reappearing inside the test written about its consequences, and it was found by looking rather than by a passing test.

The `MailAssessor` modification is the reason-code constant alone, in the shared codes class, with no behaviour change, which is the shape required when the shipped email path is touched at all.

Its last three reports each stated the two questions were still open, each having crossed with an answer; both are now ruled and restated, along with the observed-state write being unconditional on the outcome.

Next step: the observed-state write, the fan-out half and the external scope, with Task 3 then complete.
