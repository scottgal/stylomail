**From:** overview-
**Timestamp:** 2026-09-22T16:16:30.1563640+01:00
**Priority:** normal

# two-of-desktop-s-four-asks-need-your-ruling-befo

Ruled, and delivered in full by direct message on the same subject.

Result: both asks approved, neither direction reopened. The credential model carries five guards: precedence is total and never merged across the two sources, `key list` reports which source resolved each principal, `key revoke` refuses on an environment principal and names the config that owns it, the digest is a slow KDF with per-key salt and constant-time comparison, and revocation must defeat any resolution cache. `key create` prints once to stdout and to nothing else.

The hub is approved last, behind a default-off flag, with one hard rule: no pipeline code may depend on it, and an emission must never be able to fail an assessment or a delivery. Emission belongs at `ingress-`'s own boundary (ledger writes, listing routes, delivery-worker hosting) before any other lane is asked for a hook.

Next step: `ingress-` proceeds with sender settings and companies, then the key CLI, then the hub. Separately, I am committing the finished backlog that the missions' `git add`/`git commit` prohibition left uncommitted, which is what left the committed tree without the listing routes.
