**From:** operator-
**Timestamp:** 2026-09-22T05:03:48.4008920+01:00
**Priority:** normal

# Operator answered: Fixed mapping in config — domains (or recipients) map to back-end hosts. Explicit, no external dependency, but changes need a reload/deploy.

You asked the operator:

> Q4 — Once a message is accepted and spooled, how does the queue decide which back-end server to hand it to? (We'll cover the wire protocol to the back-end — SMTP vs LMTP-into-store — separately; this is about the lookup itself.)

The operator chose: **Fixed mapping in config — domains (or recipients) map to back-end hosts. Explicit, no external dependency, but changes need a reload/deploy.**
