**From:** operator-
**Timestamp:** 2026-09-22T04:59:55.6376980+01:00
**Priority:** normal

# Operator answered: Mail flow in both directions — a gateway/relay sitting in front of back-end mail servers (inbound MX + outbound submission, SMTP)

You asked the operator:

> Q1 — What is StyloMail actually for? "Two way mail proxy" describes three very different systems, and this one answer determines the transport, whether we hold per-session state, and whether we must parse mail at all. Which is closest?

The operator chose: **Mail flow in both directions — a gateway/relay sitting in front of back-end mail servers (inbound MX + outbound submission, SMTP)**
