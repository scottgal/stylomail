**From:** operator-
**Timestamp:** 2026-09-22T05:03:02.5779850+01:00
**Priority:** normal

# Operator answered: Security/edge boundary — TLS+STARTTLS termination, auth, rate limits, reputation. Back-ends must never be directly reachable.

You asked the operator:

> Q2 — What does StyloMail actually decide that a plain relay wouldn't? A proxy's routing lookup *is* its product — a bare MTA would just deliver per DNS. Why does the proxy sit in front of those back-end servers?

The operator chose: **Security/edge boundary — TLS+STARTTLS termination, auth, rate limits, reputation. Back-ends must never be directly reachable.**
